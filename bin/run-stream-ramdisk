#!/bin/bash
# runs the data streaming backend
if [ "$EUID" -ne 0 ]
  then echo "Please run as root"
  exit
fi
cd PROJECTS/AFHBA404
while [ "x$1" != "x" ]; do
	export $1
	shift	
done
./scripts/brotto-stream-ramdisk


